--Drop table
Drop TABLE library_admin;

--Create table
CREATE TABLE library_admin
(
    user_id NUMBER(5) GENERATED BY DEFAULT ON NULL AS IDENTITY START WITH 1 PRIMARY KEY,
    username VARCHAR2(20) NOT NULL,
    password VARCHAR2(10) NOT NULL
);

--Insert data
INSERT INTO library_admin (username, password) VALUES ('htar','123');
